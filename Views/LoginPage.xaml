<UserControl x:Class="BD.Views.LoginPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BD.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5">
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="1">
            <StackPanel>
                <TextBox x:Name="LoginB" Margin="0,75,0,40">
                    
                </TextBox>
                <PasswordBox x:Name="PasswordB" Margin="0,75,0,40" PasswordChanged="Password_Changed">
                    
                </PasswordBox>
                <Button Content="Submit" MaxWidth="100" Click="OnLoginSubmit_click"/>
            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>
