<UserControl x:Class="BD.Views.LoginPage"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:BD.Views"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Margin="5" >
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="2*"/>
            <ColumnDefinition Width="3*"/>
            <ColumnDefinition Width="2*"/>
        </Grid.ColumnDefinitions>

        <DockPanel Grid.Column="1" Background="#434343">
            <StackPanel Margin="10,0,10,0">
                <TextBlock Text="Username:" HorizontalAlignment="Center" Margin="0,60,0,10" Foreground="#dfdfdf"/>
                <TextBox x:Name="LoginB" KeyDown="OnLoginSubmit_key"/>
                <TextBlock Text="Password:" HorizontalAlignment="Center" Margin="0,25,0,10" Foreground="#dfdfdf"/>
                <PasswordBox x:Name="PasswordB" PasswordChanged="Password_Changed" KeyDown="OnLoginSubmit_key"/>
                <Button Content="Submit" Margin="0,30,0,0" MaxWidth="100" Click="OnLoginSubmit_click"/>
                

            </StackPanel>
        </DockPanel>
    </Grid>
</UserControl>
